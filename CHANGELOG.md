# Changelog

All notable changes to `canvas` are documented here. Format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); this project adheres
to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial package scaffold: `Ruxen.toml` (library + `[system_libs]` for
  SDL2/Skia), API skeleton (`Window`, `Canvas`, `Color`, `Rect`, `Event`),
  the `runtime/skia_shim.c` C-shim placeholder, and full design docs
  (`DESIGN`, `ARCHITECTURE`, `FFI`, `ROADMAP`).
